# Part 4 Formatter Agent

This Part 4 folder has been intentionally cleared for reassignment to another student.

Agent responsibility (to be implemented by the next student):
- Format verified shared research state into a markdown or HTML research report.
- Save the report locally and update shared state with output metadata.

Tool responsibility:
- Implement `save_report_file(content, filename, file_type, output_dir)` or equivalent.

Input state fields:
- `topic`
- `summary`
- `verified_claims`
- `rejected_claims`
- `sources`
- `output_format`
- `logs`

Expected output state fields:
- `output_file_path`
- `logs`

Tests placeholder:
- `src/components/part_4_formatter/tests/test_formatter_agent.py`
- `src/components/part_4_formatter/tests/test_formatter_tools.py`

Student contribution proof:
- The Part 4 implementation has been removed so the next student can add their own code via Git.
